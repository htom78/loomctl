# Platform Staging Checkpoint

Date: 2026-07-07

This checkpoint records the current external staging handoff state for the multi-user online sandbox, harness loop, VAS-lite, brain feedback, and `agent-git-service` control-plane path. It is a handoff note, not production-cutover proof.

## Generated Artifacts

- `plan.json`: token-free platform cutover plan for external LiteLLM, Coder, and `agent-git-service` targets.
- `cutover-bundle/`: token-free operator bundle with `manifest.json`, `env.sh`, `external-secrets.json`, `github-actions-staging.yml`, `commands.sh`, `staging-ci.json`, and `upstream-agent-git-service.json`.
- `cutover-bundle/reports/bundle-verify.json`: green bundle integrity, manifest, hash, strict-command, and secret-scan proof.
- `cutover-bundle/reports/staging-prerequisites.json`: red prerequisite proof for this local shell.
- `cutover-bundle/reports/staging-targets-plan.json`: green token-free replacement plan for placeholder external staging targets.
- `cutover-bundle/reports/staging-targets-env-check.json`: red token-free validation that the `LOOM_REAL_*` target env names are not present in this local shell.
- `cutover-bundle/reports/upstream-agent-git-service-server-env-plan.json`: green token-free upstream `gh-server` env/start/readiness plan.
- `cutover-bundle/reports/external-staging-audit.json`: red pre-serve audit rollup.
- `cutover-bundle/reports/operator-status.json`: current operator rollup and next commands.
- `cutover-bundle/reports/operator-handoff-packet.json`: token-free external operator handoff packet with current inputs, command refs, report refs, and provider summaries.
- `cutover-bundle/reports/ci-handoff-install.json`: green local GitHub Actions workflow install report.
- `cutover-bundle/reports/ci-handoff-preflight.json`: red GitHub Actions handoff preflight report with green local repo-root workflow install evidence.
- `cutover-bundle/reports/ci-workflow-publish-plan.json`: red token-free workflow publish plan; it needs real `--repo <owner/repo>` and `--ref <branch>`.
- `cutover-bundle/reports/ci-secrets-plan.json`: green token-free GitHub Actions secret setup plan.
- `cutover-bundle/loom-wrapper`: local source-tree `LOOM_BIN` wrapper for running bundle commands without a global `loom` install.
- `/Users/eno/Documents/New project/.github/workflows/github-actions-staging.yml`: installed copy of the bundle workflow template at the current git root.

## Current Green Evidence

- Bundle payload is token-free and hash-anchored.
- `operator-status.json` now includes `reports.bundleVerify`, so bundle integrity is visible from the single cockpit before strict pre-serve.
- GitHub Actions staging workflow template exists in the bundle and has the strict same-ref/same-phase concurrency guard.
- The GitHub Actions workflow has been installed locally under the current git root's `.github/workflows/github-actions-staging.yml`, and `ci-handoff-install.json` proves its sha256 matches the bundle template.
- `ci-handoff-preflight.json` now also checks `--repo-root "/Users/eno/Documents/New project"` and proves the installed workflow sha256 matches the bundle template before any real Actions dispatch.
- `external-secrets.json` lists only required env names, not values.
- `upstream-agent-git-service-server-env-plan.json` now gives `DB_DSN="${DB_DSN:?missing DB_DSN}" gh-server` plus the external `/readyz` probe without reading or storing `DB_DSN`.
- Local source-tree wrapper exists at `cutover-bundle/loom-wrapper`; `commands.sh` and `staging-prerequisites.json` now use it automatically when `LOOM_BIN` is unset and no global `loom` command is available.
- `jq` is available.
- Strict target inventory is token-free and now records placeholder gaps explicitly instead of treating sample endpoints as real staging.
- `staging-targets-plan.json` emits only env names, an env validation command, `applyCommandArgs`, `inputTemplatePath`, `inputTemplate`, `applyInputCommandArgs`, a `jq` patch command, `realTargetsCheckCommandArgs`, and `validatedRealTargetsShellCommand` for writing plus proving `plan.real-targets.json`; it uses `cutover-bundle/loom-wrapper` for those harness commands when `LOOM_BIN` is unset, and it does not read or store real endpoint/token values. Prefer `platform-staging-targets-apply`: it validates `LOOM_REAL_*` or the local `--input` JSON file, writes `plan.real-targets.json`, writes `reports/real-staging-targets.json`, and emits only token-free paths, sha256 values, gates, env names, and input path/sha plus `inputProof` when used; failed input-file applies now mirror `inputProof.missing[]` into top-level `input.*` missing keys so cockpit handoff points at the field to repair.
- `staging-targets-env-check.json` reports only env names, shape checks, and placeholder flags, never the actual replacement target values.
- `staging-prerequisites.json`, `external-staging-audit.json`, and `ci-handoff-preflight.json` now emit wrapper-backed nextActions for target replacement, upstream AGS server env planning, workflow install, workflow publish planning, and GitHub secret planning when `LOOM_BIN` is unset.
- `operator-status.json` now lifts the external audit's env and staging target replacement actions, prerequisites actions, and `staging-targets-env-check.json` actions above CI handoff actions, so placeholder replacement, replacement env validation, and env gaps stay visible before dispatching Actions.
- `operator-status.json` now emits CI handoff install/preflight/run/proof/dispatch/wait/artifact harness command arrays through `cutover-bundle/loom-wrapper` when `LOOM_BIN` is unset, while leaving `gh workflow run` commands on `gh`.
- `operator-status.json` now also carries `reports.stagingRun`, `reports.stagingTargets`, and `reports.stagingEvidence` refs; in this local shell they exist as missing report refs, making the pre-serve evidence gap visible from the same cockpit before any real staging run.
- `operator-status.json` now turns those missing pre-serve evidence refs into a concrete next action that names `staging-run.json`, `staging-targets.json`, and `staging-evidence.json` plus the strict pre-serve command that should generate them.
- `operator-status.json` now includes `blockingGroups[]` for `local-mvp`, `operator-env`, `external-targets`, `upstream-agent-git-service`, `pre-serve-evidence`, and `github-actions`, making the remaining real-staging and original multi-user/multi-agent MVP blockers machine-readable instead of prose-only. The `local-mvp` group keeps `visionLock`, `onlineSandbox`, and `concurrencyAudit` gaps visible from `platform-goal-audit` so clearing real staging targets does not hide the original design goal; when the plan has cutover/smoke stages, it also exposes the runner command for those reports before the concurrency audit command.
- `operator-status.json` now includes `operator-env.envRefs[]` with token-free env names, `requiredFor`, and whitelisted `uses` metadata, so UI/CI can render the missing secret inventory without parsing prose or handling secret values.
- `operator-status.json` now includes `external-targets.targetEnvRefs[]` with the `LOOM_REAL_*` env names, target field names, placeholder ids, presence status, and token-free env-check snippets for real staging target replacement; it also includes `external-targets.targetInputRefs[]` with `real-staging-targets.input.json` template metadata, the operator apply wrapper argv, and token-free input-file validation gates. The current cockpit step advances to ready-to-run only when that input file exists, has the right schema, has all required fields, passes URL/issue/repo shape checks, and avoids placeholder targets, without reading real target values into the status report; absent files still emit an `inputFile.exists=false` summary so cockpit/handoff views can explain the blocker without inferring from paths. When the file is ready, cockpit/loop now runs `platform-operator-real-staging-targets-apply --auto-refresh-bundle` with the current input sha256 instead of the lower-level apply-only command, so one audited command applies targets, refreshes the bundle, and refreshes cockpit artifacts. `platform-operator-target-input-template` and admin-only `POST /tenants/<tenant>/operator/target-input-template` can now write the missing blank template and refresh `operator-status.json`, `operator-cockpit-plan.json`, `operator-cockpit-next.json`, and `operator-handoff-packet.json` from one status snapshot without receiving real target values.
- Browser cockpit admins can now write that `real-staging-targets.input.json` through `POST /tenants/<tenant>/operator/real-staging-target-input`; headless operators can run `loom harness platform-operator-real-staging-target-input --dir cutover-bundle --input <platform-staging-targets-input.json> --repo-root <repo-root> --repo <owner/repo> --ref <branch>` for the same bundle-local write and refreshed cockpit artifacts without putting target values in the output report. The cockpit submits the currently rendered input file sha256 as `expectedInputSha256`, stale writes return token-free `409 platform-operator-input-write-conflict/v1` without overwriting the file, and successful writes refresh `reports/operator-status.json`, `reports/operator-cockpit-plan.json`, `reports/operator-cockpit-next.json`, and `reports/operator-handoff-packet.json` from the same status snapshot with the current proof flags and GitHub target. The response and tenant audit include only path, sha256, byte length, field names, token-free input-file validation summary, refreshed report paths, handoff path, and token-free handoff metadata, not the submitted target values.
- Browser cockpit admins can now apply the current `real-staging-targets.input.json` form through the cockpit **Apply input file** button; the button first writes the current form values through `POST /tenants/<tenant>/operator/real-staging-target-input`, then calls `POST /tenants/<tenant>/operator/real-staging-targets-apply` with the returned input sha256 and `autoRefreshBundle: true`. Headless operators can run the equivalent `loom harness platform-operator-real-staging-targets-apply --dir cutover-bundle --auto-refresh-bundle --repo-root <repo-root> --repo <owner/repo> --ref <branch>`. The route and CLI wrapper reject stale input sha256 values, write `reports/staging-targets-apply.json`, `plan.real-targets.json`, and `reports/real-staging-targets.json`, refresh operator status/plan/next/handoff artifacts, and with `autoRefreshBundle` consume the apply report, update `plan.json`/manifest/upstream AGS guide plus sanitized target reports, and advance the cockpit from `real-staging-targets` to the next blocking group without requiring `shell.exec`. The HTTP response, CLI report, and tenant audit expose only paths, sha256 values, `inputProof`, gates, missing keys, refresh metadata, and refreshed report metadata; actual target values remain confined to the local input/real-plan files. Browser cockpit admins can still use **Save input file** as a draft save and `POST /tenants/<tenant>/operator/bundle-refresh` or the cockpit **Refresh bundle** button as a manual retry path with stale apply-report sha256 protection.
- `operator-status.json` now includes `upstream-agent-git-service.serverEnvRefs[]` with the required upstream `gh-server` env names, presence status, and token-free env-check snippets.
- `operator-status.json` now includes `agentGitService`, a token-free provider contract summary over upstream server-env plan, upstream handoff, staging readiness, optional native-write proof, and compat artifacts, and the `upstream-agent-git-service` blocking group includes missing handoff/readiness/compat evidence plus audited AGS command refs while those pre-serve reports are absent or red, including a focused `ags-evidence-import` ref for downloaded AGS pre-serve artifacts.
- `operator-status.json` now includes `github-actions.secretRefs[]` with required GitHub Actions secret names, presence status, and token-free `gh secret set` command args from CI preflight.
- `blockingGroups[].commandRefs` now exposes token-free command arrays for local MVP cutover/smoke plus concurrency audit, external-target replacement, upstream AGS start/readyz/handoff/evidence import, plan-derived AGS staging readiness and compat rehearsal, strict pre-serve, and GitHub Actions handoff steps, so UI/CI can execute audited commands without parsing `nextActions`.
- `operator-status.json` now treats AGS native write as a first-class post-serve proof when the plan or staging CI requires `agent-git-service-native-write-check`; the AGS summary exposes `nativeWriteCheckRequired`, `reports.nativeWriteCheck`, and `gates.nativeWriteCheckOk`, while the `post-serve-proof` blocking group exposes the missing proof, uses the plan-derived native-write command as the default runnable proof command, and keeps focused `ags-evidence-import --phase post-serve` for downloaded native-write evidence.
- `operator-status.json` now includes `cockpitPlan` (`platform-operator-cockpit-plan/v1`), an ordered token-free handoff plan that connects those refs into `real-staging-targets`, `upstream-agent-git-service`, `operator-env`, `github-actions`, `pre-serve-evidence`, and `post-serve-proof` steps.
- `cockpitPlan.execution` now exposes `state`, pending step count, missing input count, command ref count, current step id, `nextInputRefs[]` for missing current-step inputs, and `nextCommandRef` for ready-to-run steps, so the future cockpit/CI runner can collect inputs or execute the next audited command.
- `operator-cockpit-next.json` now exports the compact `platform-operator-cockpit-next/v1` next-action artifact with current state, current step id, current blocking group id, plus `inputRefs` or `commandRef`, for frontends and CI runners that should not parse the full plan.
- `platform-operator-handoff-packet` now exports a token-free `platform-operator-handoff-packet/v1` artifact that bundles the status summary, compact cockpit next action, all missing input refs, flattened command refs from every current blocking group, GitHub target/secret refs, AGS provider summary, blocking groups, report refs, and next actions into one external operator handoff JSON; each flattened command ref carries `stepId`, `blockingGroupId`, `runnerCommandArgs`, and `runnerExecuteCommandArgs` so UI/agent executors can route work without re-parsing the full cockpit plan or reconstructing runner selectors. Non-cockpit checklist groups such as `local-mvp` use their group id as `stepId`. `platform-operator-cockpit-loop` refreshes this packet automatically beside the status/plan/next/runner reports.
- `platform-operator-handoff-runner` now consumes `operator-handoff-packet.json`, selects a flattened command ref by `--blocking-group` and `--label`, dry-runs by default, and executes only with `--execute` under a bundle-local lease. This lets operators run non-current checklist commands such as `local-mvp` cutover/smoke proof without bypassing the cockpit input gate.
- `operator-handoff-packet.json.handoff.blockingGroupIds` now mirrors every current `operator-status.json.blockingGroups[].id`, not only groups with missing inputs, so command-only blockers such as pre-serve evidence and GitHub/AGS proof steps stay visible in external handoff.
- `platform-operator-approvals` is now shared by CLI and HTTP, exporting `platform-operator-approvals/v1` with missing/failed/gate/stage/requirement buckets plus `nextActions` from `staging-ci.json.operatorApprovals[]`.
- Generated strict bundles now write `reports/operator-handoff-packet.json` during pre-serve and final post-serve cockpit refreshes, list it in `staging-ci.expectedReports.postServe` for CI archive/import, and validate its final schema in post-serve jq checks. The post-serve artifact allowlist now also includes `goal-audit.json`, while `operator-artifacts.json` still excludes goal-audit/cockpit/handoff files from hash-anchor requirements because those final reports are generated after artifact verification.
- The harness server now exposes admin-only `GET /tenants/<tenant>/operator/handoff-packet` and `GET /tenants/<tenant>/operator/approvals` export paths that write their reports, do not require `shell.exec`, and record token-free audit metadata for external operator handoff and approval review.
- The browser cockpit now exposes **Export handoff** and **Export approvals** controls wired to those admin-only endpoints, passing `clientId` and displaying the token-free JSON in the report pane without requiring `shell.exec`.
- `platform-operator-cockpit-runner` now consumes that compact artifact, returns missing `inputRefs` as a nonzero token-free report, dry-runs ready commands by default, and executes `commandRef.commandArgs` only with explicit `--execute`.
- `platform-operator-cockpit-loop` now refreshes status/plan/next/handoff-packet/runner artifacts in one outer loop, includes a token-free status summary with `agentGitService` and `blockingGroups`, and stops on missing inputs or dry-run by default while advancing ready commands only with explicit `--execute`.
- The harness server now serves a minimal browser cockpit at `GET /operator-cockpit?tenant=<tenant>&repo=<owner/repo>&ref=<branch>` with token scrubbing, explicit CI target inputs, refresh, confirm, and execute controls wired to the operator cockpit endpoints.
- The browser cockpit now has **Seed target template**, wired to the admin-only `target-input-template` endpoint, so an operator can create `real-staging-targets.input.json` and refresh cockpit artifacts before filling real target values, without granting shell execution.
- The browser cockpit now renders the AGS provider summary and the full blocking group checklist above the current input/action panel, so the original multi-user external cutover target stays visible while clearing one local step.
- The browser cockpit blocking group checklist now expands token-free env, server-env, target-env, target-input, GitHub target, and GitHub secret refs with presence status plus copyable export/apply/secret commands, so operators can work ahead on non-current production blockers without reading raw `operator-status.json`.
- The browser cockpit now renders the current CI target plus missing `inputRefs` as an operator checklist with GitHub `repo`/`ref`, target, placeholder, input-template, env-check, copyable export/apply/check snippets, and copyable ready command text, so the real-staging handoff no longer requires parsing raw JSON.
- The browser cockpit's top-level GitHub `repo`/`ref` fields can now be persisted immediately with **Save CI target**, writing the same admin-only `github-actions-target.input.json` file and refreshing `reports/operator-status.json`, `reports/operator-cockpit-plan.json`, `reports/operator-cockpit-next.json`, and `reports/operator-handoff-packet.json` without waiting for the later GitHub Actions cockpit step.
- Browser cockpit admins can now write `github-actions-target.input.json` through `POST /tenants/<tenant>/operator/github-actions-target-input`; the cockpit submits the current target input sha256 when it has one, stale repo/ref writes return token-free `409 platform-operator-input-write-conflict/v1` without overwriting the file, and successful writes stay inside the operator bundle, reject placeholder repos, refresh the operator status/plan/next/handoff artifacts from one status snapshot with the current proof flags and GitHub target, and return/audit only the repo/ref target, path, sha256, byte length, field names, refreshed report paths, handoff path, and token-free handoff metadata. CLI operators can use `platform-operator-github-actions-target-input --repo <owner/repo> --ref <branch>` for the same token-free file write and refreshed cockpit artifacts without starting the browser cockpit.
- Browser cockpit admins can now import already-downloaded GitHub Actions report artifacts through `POST /tenants/<tenant>/operator/ci-artifact-import`; the route requires admin access, does not require `shell.exec`, only accepts artifact directories inside the operator bundle, reuses `platform-ci-artifact-import` allowlist/phase/run-id/runner-proof validation, writes `reports/ci-artifact-import.json`, refreshes the operator status/plan/next/handoff artifacts from one status snapshot, returns refreshed report paths plus token-free `artifactImportOk`, `status`, and `handoff` summaries, and audits only token-free path/count/gate metadata. The response top-level `ok` is now completion-aware: imported reports, refreshed operator status, and handoff packet must all be green; a full post-serve import including `goal-audit.json` now drives the HTTP response, cockpit next artifact, and handoff packet to `production-cutover-ready`/`complete`.
- The operator cockpit now defaults the CI artifact phase from the refreshed status instead of staying fixed on pre-serve: `prepare-pre-serve` keeps `pre-serve`, while `ready-for-serve`, `run-post-serve-proof`, and `production-cutover-ready` select `post-serve` unless the operator explicitly chose a phase or supplied `artifactPhase` in the URL.
- `platform-ci-artifact-import` now also semantically rejects failed `bundle-verify.json`, `staging-prerequisites.json`, `staging-targets.json`, `platform-preflight.json`, `staging-run.json`, `external-staging-audit.json`, `staging-evidence.json`, `staging-verdict.json`, `serve-ready.json`, `cutover-report.json`, `smoke.json`, `concurrency-audit.json`, `*-env.json` platform cutover environment evidence, `safe-run.json`/`*-run.json` platform cutover run evidence, `operator-artifacts.json`, `operator-approvals.json`, `staging-proof.json`, `goal-audit.json`, `operator-cockpit-plan.json`, `operator-cockpit-next.json`, `operator-handoff-packet.json`, `upstream-agent-git-service-server-env-plan.json`, `upstream-agent-git-service-handoff.json`, `agent-git-service-staging-readiness.json`, `agent-git-service-compat/*`, and `agent-git-service-native-write-check.json` artifacts instead of copying merely valid JSON, clearing stale destination files and preserving token-free import output when downloaded evidence is red. If `staging-ci.json` checks require AGS for `cutover-report.json` or `smoke.json`, import treats AGS as required even when the downloaded report omits the AGS marker fields. AGS compat manifest, compat candidate probe, compat comparison candidate summary, readiness, and native-write imports bind their candidate/readiness/write targets back to `staging-ci.externalTargets`, including AGS base URL and native-write issue/repo/wiki/workspace/attachment/wiki-note evidence. In AGS-required smoke imports, `agentGitServiceNativeProjectionChecked: true` is not sufficient unless `onlineSandboxGoldenPathCapabilities` also includes `agent-git-service-native-projection`; generated staging CI checks enforce the same proof. Unknown allowlisted report names are rejected by default. For post-serve imports, `operator-cockpit-plan.json` and `operator-cockpit-next.json` must be the final `production-cutover-ready` complete state rather than stale `needs-input` or `ready-to-run` cockpit artifacts.
- Generated AGS compat staging jq checks now also require baseline/candidate probes to carry the contract schema, provider, read-only `/api/v3` shape, `ok: true`, and `requestsTokenFree: true`; artifact import enforces the same probe shape before copying downloaded CI evidence. The checks also compare both `agent-git-service-compat/candidate.json.baseUrl` and `agent-git-service-compat/compare.json.candidate.baseUrl` to `staging-targets.json.targets.controlPlane.baseUrl`, after the same trailing slash and `/api/v3` normalization used for the manifest target check.
- Imported cockpit artifacts and generated staging jq checks now require non-complete `operator-cockpit-plan.json` and `operator-cockpit-next.json` evidence to carry both `currentStepId` and `currentBlockingGroupId`; plan artifacts must also have a matching `steps[]` entry with the same blocking group, so compact runner evidence cannot drift away from the full blocking-group plan.
- Imported `operator-artifacts.json` must now include non-empty `requiredReports`, empty failure buckets, and matching green sha256 report refs for every required report; a residual `{ ok: true }` or gates-only artifact is rejected and clears stale destination evidence.
- Imported `operator-approvals.json` must now match the same bundle's `staging-ci.operatorApprovals`: zero expected mutating approvals may produce an empty approved summary, while every expected approval must have an approved summary with matching stage/gate/evidence/report/command/requirements, empty failure buckets, and no next actions. The generated post-serve jq check reads both `operator-approvals.json` and `staging-ci.json`, so a gate-only approvals shell or a missing expected approval cannot satisfy CI.
- Imported `goal-audit.json` must now carry the original target lock plus report refs for the cutover, smoke, concurrency, CI proof, external staging, operator approval, cockpit runner, operator artifact, staging proof, and AGS upstream/compat evidence before its green gates count. A gates-only final goal audit is rejected and clears stale destination evidence.
- Imported `staging-proof.json` must now include non-empty `requiredReports`, empty missing/forbidden buckets, and matching green sha256 refs under `reports` for every required report; a gates-only proof can no longer satisfy the final staging proof import.
- Imported `bundle-verify.json` and generated pre-serve jq checks must now include every empty bundle verification failure bucket as an explicit string array, so a partial `ok: true` bundle verifier shell cannot become the root proof for staging.
- Imported `operator-cockpit-runner-execute.json`, local artifact verification, and generated post-serve jq checks must now include an explicit empty string-only `missing` array plus matching `currentStepId`/`currentBlockingGroupId` values on the runner report and its `executionLease` before runner execution proof can count toward production cutover.
- Generated staging jq checks for `platform-preflight.json`, `staging-proof.json`, `operator-status.json`, and `concurrency-audit.json` also require explicit empty `missing` arrays, matching the stricter importer semantics and preventing null/missing buckets from satisfying CI checks.
- Imported AGS `cutover-report.json` and `smoke.json` must now include server and tenant control-plane discovery proof; AGS-ready or smoke-passed shells without discovery fields are rejected before they can feed post-serve artifacts.
- AGS contract probes now reject empty 200 discovery shells: `/api/v3`, `/api/v3/meta`, and `/api/v3/rate_limit` must return minimal GitHub-compatible JSON shape, with token-free `invalidEndpoints` and endpoint `semanticErrors` recorded when shape checks fail.
- Imported `operator-handoff-packet.json` and generated post-serve jq checks must now see a complete `production-cutover-ready` packet with empty status missing entries, complete cockpit counters, empty handoff blocker/input/command/next-action arrays, zero pending handoff counts, empty blocking groups, workflow dispatch args, and report evidence; an `ok: true` shell with missing or inconsistent nested fields is rejected and clears stale destination evidence.
- Imported `staging-prerequisites.json` and generated pre-serve jq checks must now include an explicit empty string-only `missing` array, so the strict external staging entry gate cannot pass on a gates-only artifact with the missing bucket absent.
- Imported `staging-run.json`, `external-staging-audit.json`, `staging-evidence.json`, and `serve-ready.json` plus generated staging jq checks now require explicit empty string-only `missing` arrays, and the run/evidence reports also require explicit empty `forbiddenValueHitReports` arrays, so pre/post-serve proof cannot pass on partial green gate shells.
- Imported `staging-targets.json` and generated pre-serve jq checks must now prove the actual target shape: external model gateway URL, coder executor, external `agent-git-service` control-plane URL, AGS issue/repo/wiki refs, empty placeholders, and green target gates; a bare `externalStagingReady: true` shell is rejected.
- Local `platform-staging-evidence` and related external-staging readiness rollups now use the same strict target-shape proof, so stale or hand-written gate-only `staging-targets.json` cannot advance `externalStagingReady`.
- Imported AGS readiness, upstream server-env, upstream handoff, and native-write evidence plus generated staging jq checks must now include an explicit empty string-only `missing` array; an `ok: true` AGS artifact with the missing bucket absent is rejected and clears stale destination evidence.
- Browser cockpit admins now also have focused **Sync AGS evidence** and **Import AGS evidence** actions backed by `POST /tenants/<tenant>/operator/ags-evidence-sync` and `POST /tenants/<tenant>/operator/ags-evidence-import`; sync accepts a GitHub Actions run id, downloads the reports artifact, writes `reports/ags-evidence-sync.json`, and focused-imports AGS evidence, while import consumes an already downloaded bundle-local artifact directory. Headless operators can run `loom harness platform-operator-ags-evidence-sync --dir cutover-bundle --run-id <github-run-id> --phase pre-serve --repo-root <repo-root> --repo <owner/repo> --ref <branch>` to download the Actions artifact and focused-import AGS evidence, or run `loom harness platform-operator-ags-evidence-import --dir cutover-bundle --artifact-dir <downloaded-ags-artifact-dir> --phase pre-serve --repo-root <repo-root> --repo <owner/repo> --ref <branch>` when the artifact is already downloaded. Both paths can switch to `--phase post-serve` for native-write evidence, default to pre-serve evidence, reuse the same token-free artifact importer and AGS semantic checks, filter the selected phase to AGS-specific expected reports instead of requiring every non-AGS report in `staging-ci.expectedReports`, write `reports/ags-evidence-import.json`, refresh operator status/plan/next/handoff artifacts, expose the import report as `operator-status.json.reports.agsEvidenceImport`, return completion-aware sync/import/status/handoff summaries, and can advance the cockpit past the `upstream-agent-git-service` blocker once server-env/handoff/readiness/compat evidence is green.
- The harness server now exposes an admin-only dry-run refresh endpoint at `GET /tenants/<tenant>/operator/cockpit-loop`, backed by the configured `operatorBundleDir` or `<workspaceRoot>/cutover-bundle`, and accepts optional `repo`/`ref` query values for CI command refs.
- The harness server also exposes an admin-only confirmed `POST /tenants/<tenant>/operator/cockpit-loop` execution path for cockpit commands; it accepts optional `repo`/`ref` body values, optional `maxSteps` from 1 to 20, and optional `queue: true`, requires server `shell.exec`, writes `operator-cockpit-runner-execute.json`, and records tenant audit event `operator_cockpit_loop_executed`. `maxSteps` omitted or `1` preserves the single compact next-action execution path and refreshes the dry-run loop report with the same CI target; `maxSteps > 1` executes the refreshed cockpit loop, writes `operator-cockpit-loop.json`, writes the last executed or blocked runner proof, and audits `maxSteps`, `iterationCount`, and `executedCount`. When `queue: true` is supplied while another operator/agent holds the execution lock, the server returns token-free `202 platform-operator-cockpit-loop-queue/v1`, persists the request under bundle-local `.loom/operator-cockpit-queue/<queueId>.json`, restores those queue files on server restart, claims a queued execution through token-free `.loom/operator-cockpit-queue/<queueId>.claim.json` ownership evidence so shared-bundle server instances do not execute the same queue item, and runs claimed items automatically after the bundle execution lane becomes available.
- The harness server now exposes `GET /tenants/<tenant>/operator/cockpit-execution-status` for admin cockpit clients. It reads the bundle-local execution lock, returns token-free `idle`, `locked`, `stale`, or `invalid` state with only owner/label/step/group/timing metadata, includes queued cockpit executions with token-free claim owner/timing metadata plus the latest queued execution result, and the browser cockpit renders that status above the AGS/blocking-group panels so concurrent operators can see who is holding or waiting for the execution lane before pressing **Execute**.
- `/status` and tenant status now publish `server.concurrencyAdmission.operatorCockpitQueue`, a token-free backend contract that declares the requested backend, active backend, queue item pattern, claim pattern, persisted snapshot support, restart recovery, shared-bundle claims, stale-claim cleanup, claim TTL, and the active store. AGS status exposes only the normalized repo and Contents path; execution status carries the same coordination summary for the browser cockpit. `candidateBackends.agentGitService` reports ready only when the control-plane provider is `agent-git-service`, a base URL is configured, and an admin or tenant-scoped control-plane token is available; requesting the AGS backend falls back to `filesystem` when the candidate is not ready, no queue repo is configured, or repo/path validation fails.
- `loom harness doctor`, `platform-preflight`, `platform-cutover-plan`, and `serve` now accept `--operator-cockpit-queue-backend filesystem|agent-git-service`, plus `--operator-cockpit-queue-ags-repo <owner/repo>` and optional `--operator-cockpit-queue-ags-path <path>`; generated cutover serve argv preserves those queue backend settings, and CLI serve status shows `activeBackend=agent-git-service` once the AGS candidate and a valid queue repo/path are configured.
- Cockpit queue persistence, recovery, claim acquisition/release, snapshot rendering, and queue position calculation now live behind `OperatorCockpitQueueBackend` in `src/harness/operator-cockpit-queue-backend.ts`; the filesystem backend stores one queue/claim file per item, while the AGS backend stores the token-free queue document through the Contents API using SHA compare-and-swap. AGS repo/path inputs are normalized before URL construction, and unresolved command placeholders are never admitted to cockpit execution.
- Operator cockpit command execution now has a bundle-local atomic lease at `.loom/operator-cockpit-runner.lock`; acquired leases record token-free `currentStepId` and `currentBlockingGroupId`, concurrent CLI or HTTP executes without queueing return token-free `mode: "blocked"` evidence and HTTP `409` without running the command, browser cockpit executes submit `queue: true` by default, and expired locks are replaced with `executionLease.recoveredStale` evidence so crashed operators do not block the bundle forever.
- `platform-goal-audit` now gates production cutover on `operatorCockpitRunnerExecuteOk`, requiring confirmed token-free `operator-cockpit-runner-execute.json` lease evidence before `productionCutoverReady`.
- Local AGS rehearsal now writes a source-tree `loom-wrapper`, executes the current cockpit command through `platform-operator-cockpit-runner --execute`, stores `reports/operator-cockpit-runner-execute.json`, and reruns artifact verification with `requireOperatorCockpitRunnerExecute: true`; a local goal audit now proves `localMvpOk`, `operatorCockpitRunnerExecuteOk`, `operatorCockpitRunnerProofAnchorOk`, and `agentGitServiceProviderOk`, leaving CI proof and strict external proof as the remaining production gates.
- `loom harness doctor`, `platform-preflight`, `platform-cutover-plan`, and `serve` now accept `--operator-bundle-dir <dir>`, and generated cutover serve argv preserves the value for non-default cockpit bundle deployments.
- `operator-cockpit-plan.json` can now be exported as a standalone token-free artifact; the export command exits zero when the artifact is written, while `operator-status.json` remains the readiness gate.
- Strict pre-serve `commands.sh` now writes token-free `operator-status.json`, `operator-cockpit-plan.json`, and `operator-cockpit-next.json` before stopping for manual `serve`, so a cockpit/CI runner can read the next blocked action at the phase boundary. Strict post-serve refreshes those cockpit artifacts before final proof, executes the current `post-serve-proof` command through `platform-operator-cockpit-runner --execute` to write lease evidence, then reruns artifact verification with `--require-operator-cockpit-runner-execute`, staging proof, goal audit, and final cockpit artifacts; `staging-ci.json.expectedReports.postServe` lists the runner execute report plus both cockpit files for CI archival, and post-serve jq checks validate their schemas.
- `platform-ci-artifact-import` now semantically validates imported `ci-run-proof.json` phase and expected run id, plus `operator-cockpit-runner-execute.json` artifacts, rejecting wrong-phase or wrong-run CI proofs plus failed, blocked, incomplete, non-lease-acquired, or lease-context-mismatched runner reports instead of copying them into final evidence; rejected and missing expected reports clear stale destination files.
- `operator-status.json`, final artifact verification, and generated post-serve jq checks now require `ci-run-proof.json.phase == "post-serve"` for final CI handoff readiness, so a pre-serve Actions proof cannot satisfy production cutover; when dispatch/wait reports exist, operator status also binds the proof to the same audited GitHub Actions run id.
- `staging-ci.json` post-serve checks now reject final operator status unless `blockingGroups` is an empty array.
- `platform-goal-audit` now accepts `reports/concurrency-audit.json` only when the full concurrency gate set is green: cutover concurrency, smoke concurrency, run-scoped workspaces, workspace branch leases, multi-agent smoke, AGS cutover evidence, and AGS native projection evidence when required. This keeps the final production gate tied to the original multi-user/multi-agent sandbox target instead of trusting a broad `ok` field.
- In AGS-required mode, `platform-goal-audit` now also requires the smoke report to explicitly set `agentGitServiceNativeProjectionChecked: true` and include `agent-git-service-native-projection` in `onlineSandboxGoldenPathCapabilities` before `onlineSandboxOk` and `localMvpOk` can pass.
- `platform-goal-audit` now requires a valid post-serve `ci-run-proof.json`, requires both `operator-artifacts.json` and `staging-proof.json` to anchor the current CI proof sha256, and requires `ciRunProofHandoffOk` when audited workflow dispatch/wait reports exist, so a stale, absent, unrolled-up, or wrong-run GitHub Actions proof cannot satisfy final cutover.
- `platform-goal-audit` now also requires `operator-artifacts.json` to anchor the current `operator-cockpit-runner-execute.json` sha256 before `operatorCockpitRunnerProofAnchorOk` and `productionCutoverReady` can pass, so a stale cockpit execution lease cannot satisfy a refreshed final proof.
- `platform-operator-status` now keeps recomputed `goalAudit.missing[]` entries visible in the `post-serve-proof` blocking group as `goalAudit.*`, even if a stale `goal-audit.json` file claims `ok: true`; cockpit and handoff views therefore cannot hide unresolved final target gates behind a merely present report file.
- When goal audit is blocked by staging-proof anchor gaps such as `ciRunProofStagingProofAnchor`, `platform-operator-status` now emits the `staging-proof` command before `goal-audit` in the `post-serve-proof` command refs, so operators rerun the rollup that can actually repair the missing anchor.
- `platform-ci-handoff-preflight` now detects a missing configured GitHub CLI before running downstream probes and reports a single `github.cli` blocker with a token-free install/pass-`--gh-bin` next action, so a fresh operator machine does not misread a missing tool as bad auth, missing repo access, or missing secrets.
- `platform-ci-handoff-preflight` now accepts `--target <json>` for a token-free `platform-ci-target-input/v1` repo/ref handoff file, records only the target input path and sha256, and keeps explicit `--repo`/`--ref` as overrides.
- `platform-operator-status`, cockpit next, and handoff packet now carry the GitHub Actions target input path/sha256/template when emitted; generated preflight/handoff-run commands reuse recorded `--target <path>` unless explicit `--repo`/`--ref` overrides are supplied, while workflow dispatch/wait/sync keep using the recorded repo/ref. Status also detects a saved `github-actions-target.input.json` before preflight is rerun, removes the resolved repo/ref input blockers, and emits `--target <path>` for the next preflight/handoff command refs. `platform-operator-github-actions-target-input` writes that saved file from CLI, refreshes `operator-status.json`, `operator-cockpit-plan.json`, `operator-cockpit-next.json`, and `operator-handoff-packet.json`, and returns only token-free repo/ref target metadata plus file/report hashes. `platform-operator-real-staging-target-input` does the same for real external staging target input by copying a local `platform-staging-targets-input/v1` file into the bundle path and returning only token-free path/hash/field metadata plus the refreshed cockpit state.
- Real upstream AGS local E2E passed on 2026-07-10 through `scripts/upstream-agent-git-service-local.sh e2e-zero`. It built upstream commit `9ab722e07b0797b67da05ecb72ad3c0feae6abd3` from upstream's Dockerfile, started the real `gh-server` against disposable TiDB Zero, reached `status: ready` after 159 seconds, and produced green semantic candidate and comparison artifacts. `cutover-bundle/reports/local-upstream-agent-git-service/e2e.json` has schema `loom-local-upstream-agent-git-service-e2e/v1`, `tokenFree: true`, all five gates green, and an empty `missing` array. A targeted scan found no DSN, token, or TCP connection string in the evidence, and the ephemeral container and Git volume were removed. The artifact remains explicitly `targetClass: local-upstream-e2e` and `externalStagingEligible: false`; it proves real upstream compatibility but does not satisfy the non-loopback strict staging gate.

## Current Red Evidence

This local shell is not yet a real staging environment. The prerequisite report is expected to fail until these are supplied:

- `LOOM_MODEL_KEY`
- `LOOM_DEV_TOKEN`
- `LOOM_VIEWER_TOKEN`
- `LOOM_ADMIN_TOKEN`
- `LOOM_GITEA_WEBHOOK_SECRET`
- `LOOM_AGENT_GIT_SERVICE_TOKEN`
- `LOOM_BOB_DEV_TOKEN`
- upstream `agent-git-service` server env: `DB_DSN`

The current plan still contains placeholder external targets. Strict external staging now rejects these even though their URLs are non-loopback:

- model gateway: `https://litellm.example/v1`
- `agent-git-service`: `https://ags.example/api/v3`
- AGS issue: `team/app#1`
- AGS repo: `team/app`
- AGS native-write attachment URL: `https://evidence.example/loom/ags-native-write`

`staging-prerequisites.json` and `external-staging-audit.json` therefore report `externalTargetsReady: false` until those are replaced with real LiteLLM, Coder, AGS, issue, repo, and evidence targets.

Use the generated replacement plan before attempting strict pre-serve:

```bash
loom harness platform-staging-targets-plan --dir cutover-bundle --report cutover-bundle/reports/staging-targets-plan.json
```

Export the `LOOM_REAL_*` names listed in that report and run its `applyCommandArgs`, or write the same values to `inputTemplatePath` using `inputTemplate` manually, through the browser cockpit target-input form, or by first seeding the file with:

```bash
loom harness platform-operator-target-input-template --dir cutover-bundle --repo-root "/Users/eno/Documents/New project" --require-external-staging --require-operator-approvals --require-agent-git-service --report cutover-bundle/reports/operator-target-input-template.json
```

Then fill `real-staging-targets.input.json` with real non-placeholder values and run the cockpit/operator `platform-operator-real-staging-targets-apply --auto-refresh-bundle` command surfaced in `targetInputRefs[].applyInputCommandArgs`. The lower-level `applyInputCommandArgs` from `staging-targets-plan.json` still validates and writes `plan.real-targets.json` plus `reports/real-staging-targets.json`; the operator wrapper additionally refreshes the bundle from the real plan and refreshes cockpit artifacts in the same audited step.

GitHub CLI is authenticated locally with `repo` and `workflow` scopes, but the current git checkout has no remote. A real CI handoff still needs an explicit GitHub `--repo <owner/repo>` and `--ref <branch>` target, or the same values saved through the browser cockpit GitHub target form, before dispatching the workflow.

`ci-handoff-preflight.json` currently reports these CI handoff gaps:

- `github.repository`
- `github.workflow`
- `github.secrets`
- `github.secrets.requiredEnv`

It does not report `local.workflowInstall`; the installed workflow at `/Users/eno/Documents/New project/.github/workflows/github-actions-staging.yml` matches the bundle sha256.

`ci-workflow-publish-plan.json` currently reports these gaps:

- `github.ref`
- `github.repository`

It now includes `repoDiscoveryCommandArgs`, `repoDiscoveryCwd`, and `repoDiscoveryFields`; run that discovery command from the repo root, then rerun the publish plan with the discovered `--repo <nameWithOwner>` and `--ref <defaultBranchRef.name>`.

Missing proof after that:

- strict pre-serve `platform-preflight`, AGS compat, AGS staging readiness, staging-run, staging-targets, staging-evidence, and staging-verdict reports
- GitHub Actions run proof and imported reports
- post-serve cutover/smoke/concurrency/approval/native-write/staging-proof/goal-audit evidence

## Next Operator Command

First replace placeholder staging targets with the token-free plan above: export the `LOOM_REAL_*` names or seed and fill `real-staging-targets.input.json` with `platform-operator-target-input-template`, then run `platform-operator-real-staging-targets-apply --auto-refresh-bundle` so the real plan, strict target proof, bundle refresh, and cockpit refresh happen together.

Before starting upstream AGS, inspect the token-free server env plan:

```bash
loom harness upstream-agent-git-service-server-env-plan --dir cutover-bundle --report cutover-bundle/reports/upstream-agent-git-service-server-env-plan.json
```

From inside `cutover-bundle/`, after setting the env names above and ensuring upstream `gh-server` has `DB_DSN`:

```bash
LOOM_REQUIRE_EXTERNAL_STAGING=1 LOOM_REQUIRE_OPERATOR_APPROVALS=1 LOOM_RUN_STAGING_CI_CHECKS=1 LOOM_CUTOVER_PHASE=pre-serve ./commands.sh
```

For GitHub Actions handoff, first regenerate `cutover-bundle/reports/operator-status.json` and `cutover-bundle/reports/operator-handoff-packet.json` with the real `--repo <owner/repo>` and `--ref <branch>` target so every nested CI command ref carries the same repository/ref. In this checkout those command arrays start with `cutover-bundle/loom-wrapper`, so no global `loom` install is required.
If `github.workflow` stays missing, rerun `platform-ci-workflow-publish-plan` with that same `--repo`/`--ref`, then execute its git publish and `gh workflow view` command arrays before rerunning preflight.
Because this bundle lives under `loomctl/`, keep `--repo-root "/Users/eno/Documents/New project"` on `platform-ci-handoff-preflight`, `platform-operator-status`, `platform-ci-handoff-run`, and `platform-ci-run-proof` commands so workflow evidence and preflight `gh` repository probes use the actual repository root.
